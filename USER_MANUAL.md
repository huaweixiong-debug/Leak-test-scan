# ATEQ 泄漏测试系统 使用说明书

本文档面向现场操作人员和设备管理员，用于快速上手 ATEQ 泄漏测试系统。

## 1. 系统简介

本系统用于 ATEQ 泄漏测试设备的扫码、程序选择、测试启动、状态监控和结果记录。

主要能力：

- 配置 ATEQ 设备和扫码枪的串口通信
- 按产品型号匹配 ATEQ 测试程序
- 支持手动启动测试，或扫码后自动启动测试
- 实时显示设备状态、测试进度和结果
- 记录测试结果并支持查询、筛选、导出 CSV

系统采用前后端一体部署：

- 后端服务：`server.js`
- 前端页面：`public/index.html`、`public/settings.html`、`public/comm-config.html`、`public/query.html` 等
- 数据文件：`data/` 目录

## 2. 登录与入口地址

启动服务后，在浏览器中访问：

- 主页面：`http://127.0.0.1:3000/index.html`
- 串口配置：`http://127.0.0.1:3000/comm-config.html`
- 系统设置：`http://127.0.0.1:3000/settings.html`
- 历史查询：`http://127.0.0.1:3000/query.html`
- 扫码枪调试：`http://127.0.0.1:3000/scanner-test.html`
- 通信测试：`http://127.0.0.1:3000/ateq-test.html`

如果在同一局域网内，可将 `127.0.0.1` 替换为工控机实际 IP。

## 3. 首次使用配置

### 3.1 检查运行环境

推荐流程：

1. 安装 `Node.js 18+`
2. 在项目目录执行 `npm install`
3. 执行 `npm run doctor` 检查依赖和文件完整性
4. 执行 `npm start` 启动服务

首次启动后，系统会自动在项目目录创建 `data/` 目录。

### 3.2 配置通信端口

打开 `/comm-config.html`：

- 设置 ATEQ 设备串口参数
- 设置扫码枪串口参数
- 确认“启用”开关已打开

不同电脑的 COM 口可能不同，请以 Windows 设备管理器中的“端口(COM 和 LPT)”为准。

### 3.3 配置产品与规则

打开 `/settings.html`：

- 新增产品型号
- 设置对应的 ATEQ 程序号
- 设置二维码关键字匹配规则
- 按需要启用：
  - 扫码确认
  - 扫码匹配
  - 扫码自动启动

## 4. 日常操作流程

### 4.1 手动测试

1. 打开 `/index.html`
2. 选择操作员
3. 选择产品型号
4. 点击“启动测试”
5. 等待测试完成
6. 查看测试结果（OK / NG）

### 4.2 扫码测试

如果产品规则已启用扫码自动启动：

1. 在设备空闲时扫描产品二维码
2. 系统自动匹配产品型号
3. 系统自动启动对应测试程序
4. 测试完成后自动记录结果

注意：

- 只有当设备处于空闲状态时，扫码才会触发自动测试
- 所有扫码记录都会保存，即使未触发测试

## 5. 结果查询与导出

打开 `/query.html`：

- 可按时间、产品型号、结果码、二维码等条件筛选记录
- 可查看测试详情
- 可导出 CSV 文件用于生产报表

## 6. 重要页面说明

- `/index.html`：主操作页面，用于选择产品、启动测试、查看实时状态
- `/comm-config.html`：配置 ATEQ 和扫码枪通信参数
- `/settings.html`：管理产品型号、程序号、操作员和扫码规则
- `/query.html`：查询历史测试记录并导出
- `/scanner-test.html`：调试扫码枪通信
- `/ateq-test.html`：调试 ATEQ 设备通信

## 7. 数据存储说明

系统数据保存在 `data/` 目录，包括：

- 串口配置
- 产品配置
- 操作员信息
- 扫码记录
- 测试记录

如需备份系统配置和历史记录，可在服务停止后复制整个 `data/` 目录。

## 8. 常见问题排查

### 8.1 页面打不开

- 确认服务已启动
- 确认访问地址和端口正确
- 确认本机访问 `http://127.0.0.1:3000`

### 8.2 ATEQ 设备离线

- 打开 `/comm-config.html` 检查 ATEQ 配置
- 确认设备已上电
- 确认 COM 口与实际接线一致
- 检查串口线是否松动

### 8.3 扫码枪无响应

- 打开 `/comm-config.html` 检查扫码枪配置
- 打开 `/scanner-test.html` 调试扫码状态
- 确认扫码枪型号对应的通信方式正确

### 8.4 扫码后没有自动启动

- 打开 `/settings.html` 检查产品规则
- 确认二维码能匹配到对应产品
- 确认产品已启用“扫码自动启动”
- 确认测试时设备处于空闲状态

### 8.5 历史记录为空

- 确认已完成过至少一次测试
- 打开 `/query.html` 查询最新记录
- 检查服务是否正常写入 `data/` 目录

## 9. 运维建议

- 更换电脑后，重新配置 COM 口
- 定期备份 `data/` 目录
- 不要手动修改 `data/` 内数据库文件，除非明确了解操作后果
- 若服务异常，优先查看 `server.log`、`server.out`、`server.err`
- 若端口被占用，先停止旧进程后再重新启动服务

## 10. 简明操作清单

1. 启动服务
2. 打开主页面 `/index.html`
3. 先到 `/comm-config.html` 配置通信
4. 再到 `/settings.html` 配置产品和扫码规则
5. 进行手动测试或扫码测试
6. 到 `/query.html` 查询并导出结果
